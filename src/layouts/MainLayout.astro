---
// src/layouts/MainLayout.astro
import '@fontsource/inter/400.css';
import '@fontsource/inter/600.css';
import '@fontsource/inter/700.css';
import CookieBanner from '../components/CookieBanner.astro';
import FloatingCta from '../components/FloatingCta.astro';
import Breadcrumbs from '../components/Breadcrumbs.astro';
import { buildBreadcrumbs, type BreadcrumbCrumb } from '../lib/breadcrumbs';
import { FB_PAGE_BIFODE } from '../lib/communityLinks';

type FooterLink = { href: string; label: string; external?: boolean; cta?: boolean };
const {
  title = 'Startseite',
  description = 'BiFoDe e.V. entwickelt Bildungs- und Präventionsformate für Schulen und Partner in NRW',
  lang = 'de',
  titleSuffix = true,
  mainClass = '',
  breadcrumbs: breadcrumbsOverride
} = Astro.props;

const isEn = lang === 'en';
const locale = isEn ? 'en' : 'de';
const currentPath = Astro.url.pathname.replace(/\/$/, '') || '/';
const breadcrumbItems =
  breadcrumbsOverride ?? buildBreadcrumbs(currentPath, locale, title);

type NavHref = { href: string; label: string; external?: boolean };
type NavGroup = { kind: 'group'; label: string; items: NavHref[] };
type NavLink = { kind: 'link'; href: string; label: string; external?: boolean };
type NavEntry = NavLink | NavGroup;

const navStructure: NavEntry[] = isEn
  ? [
      { kind: 'link', href: '/en', label: 'Home' },
      {
        kind: 'group',
        label: 'About',
        items: [
          { href: '/en/about', label: 'About us' },
          { href: '/en/our-work', label: 'Our work' },
          { href: '/en/cooperation', label: 'Cooperation' }
        ]
      },
      {
        kind: 'group',
        label: 'Programmes',
        items: [
          { href: '/en/schools', label: 'Schools' },
          { href: '/en/projects/ulpan-next', label: 'Ulpan Next' },
          { href: '/en/moodle', label: 'Moodle' }
        ]
      },
      {
        kind: 'group',
        label: 'Projects',
        items: [
          { href: '/en/projects', label: 'Overview' },
          { href: '/en/projects/ulpan-next', label: 'Ulpan Next' },
          { href: '/en/moodle', label: 'Moodle' }
        ]
      },
      {
        kind: 'link',
        href: FB_PAGE_BIFODE,
        label: 'Dates',
        external: true
      },
      {
        kind: 'group',
        label: 'Join',
        items: [
          { href: '/en/membership', label: 'Membership' },
          { href: '/en/donate', label: 'Donate' }
        ]
      },
      { kind: 'link', href: '/en/contact', label: 'Contact' }
    ]
  : [
      { kind: 'link', href: '/', label: 'Start' },
      {
        kind: 'group',
        label: 'Verein',
        items: [
          { href: '/ueber-uns', label: 'Über uns' },
          { href: '/unsere-arbeit', label: 'Arbeit' },
          { href: '/kooperation', label: 'Kooperation' }
        ]
      },
      {
        kind: 'group',
        label: 'Angebote',
        items: [
          { href: '/fuer-schulen', label: 'Schulen' },
          { href: '/projekte/ulpan-next', label: 'Ulpan Next' },
          { href: '/moodle', label: 'Moodle' }
        ]
      },
      {
        kind: 'group',
        label: 'Projekte',
        items: [
          { href: '/projekte', label: 'Übersicht' },
          { href: '/projekte/ulpan-next', label: 'Ulpan Next' },
          { href: '/moodle', label: 'Moodle' }
        ]
      },
      {
        kind: 'link',
        href: FB_PAGE_BIFODE,
        label: 'Termine',
        external: true
      },
      {
        kind: 'group',
        label: 'Mitmachen',
        items: [
          { href: '/mitglied-werden', label: 'Mitgliedschaft' },
          { href: '/spenden', label: 'Spenden' }
        ]
      },
      { kind: 'link', href: '/kontakt', label: 'Kontakt' }
    ];

const footerBrandText = isEn
  ? 'Education forum for democracy and diversity in NRW. Education, prevention, and exchange for a democratic society.'
  : 'Bildungsforum für Demokratie und Vielfalt NRW. Bildung, Prävention und Austausch für eine demokratische Gesellschaft.';

const footerOrganisation: FooterLink[] = isEn
  ? [
      { href: '/en/about', label: 'About' },
      { href: '/en/our-work', label: 'Our Work' },
      { href: '/en/cooperation', label: 'Cooperation' }
    ]
  : [
      { href: '/ueber-uns', label: 'Über uns' },
      { href: '/unsere-arbeit', label: 'Unsere Arbeit' },
      { href: '/kooperation', label: 'Kooperation' }
    ];

const footerOffers: FooterLink[] = isEn
  ? [
      { href: '/en/schools', label: 'Schools' },
      { href: '/en/projects', label: 'Projects' },
      { href: '/en/projects/ulpan-next', label: 'Ulpan Next' },
      { href: '/en/moodle', label: 'Moodle' }
    ]
  : [
      { href: '/fuer-schulen', label: 'Schulen' },
      { href: '/projekte', label: 'Projekte' },
      { href: '/projekte/ulpan-next', label: 'Ulpan Next' },
      { href: '/moodle', label: 'Moodle' }
    ];

const footerCurrent: FooterLink[] = isEn
  ? [
      { href: '/en/news', label: 'News' },
      { href: FB_PAGE_BIFODE, label: 'Events', external: true },
      { href: '/en/registration', label: 'Registration' }
    ]
  : [
      { href: '/aktuelles', label: 'Aktuelles' },
      { href: FB_PAGE_BIFODE, label: 'Termine', external: true },
      { href: '/anmeldung', label: 'Anmeldung' }
    ];

const footerJoin: FooterLink[] = isEn
  ? [
      { href: '/en/membership', label: 'Become a member', cta: true },
      { href: '/en/donate', label: 'Donate', cta: true }
    ]
  : [
      { href: '/mitglied-werden', label: 'Mitglied werden', cta: true },
      { href: '/spenden', label: 'Spenden', cta: true }
    ];

const footerLegalLinks: FooterLink[] = isEn
  ? [
      { href: '/en/legal-notice', label: 'Legal Notice' },
      { href: '/en/statutes', label: 'Statutes' },
      { href: '/en/privacy', label: 'Privacy Policy' }
    ]
  : [
      { href: '/impressum', label: 'Impressum' },
      { href: '/satzung', label: 'Satzung' },
      { href: '/datenschutz', label: 'Datenschutz' }
    ];

const footerDisclaimer = isEn
  ? 'Our programmes provide education, orientation and general information. They do not replace individual legal, tax or immigration advice. Where appropriate, we refer you to competent authorities or qualified professionals.'
  : 'Unsere Angebote dienen der Bildung, Orientierung und allgemeinen Information. Sie ersetzen keine individuelle Rechts-, Steuer- oder Einwanderungsberatung. Bei Bedarf verweisen wir auf zuständige Stellen oder qualifizierte Fachpersonen.';

const pathMap: Record<string, string> = {
  '/': '/en',
  '/ueber-uns': '/en/about',
  '/unsere-arbeit': '/en/our-work',
  '/fuer-schulen': '/en/schools',
  '/schulen': '/en/schools',
  '/projekte': '/en/projects',
  '/projekte/ulpan-next': '/en/projects/ulpan-next',
  '/moodle': '/en/moodle',
  '/kooperation': '/en/cooperation',
  '/aktuelles': '/en/news',
  '/termine': '/en/events',
  '/anmeldung': '/en/registration',
  '/mitglied-werden': '/en/membership',
  '/kontakt': '/en/contact',
  '/spenden': '/en/donate',
  '/beitrittserklaerung': '/en/membership-declaration',
  '/satzung': '/en/statutes',
  '/impressum': '/en/legal-notice',
  '/datenschutz': '/en/privacy',
  '/cookie-richtlinien': '/en/cookie-policy',
  '/en': '/',
  '/en/about': '/ueber-uns',
  '/en/our-work': '/unsere-arbeit',
  '/en/schools': '/fuer-schulen',
  '/en/projects': '/projekte',
  '/en/projects/ulpan-next': '/projekte/ulpan-next',
  '/en/ulpan': '/projekte/ulpan-next',
  '/en/moodle': '/moodle',
  '/en/cooperation': '/kooperation',
  '/en/news': '/aktuelles',
  '/en/events': '/termine',
  '/en/registration': '/anmeldung',
  '/en/membership': '/mitglied-werden',
  '/en/contact': '/kontakt',
  '/en/donate': '/spenden',
  '/en/membership-declaration': '/beitrittserklaerung',
  '/en/statutes': '/satzung',
  '/en/legal-notice': '/impressum',
  '/en/privacy': '/datenschutz',
  '/en/cookie-policy': '/cookie-richtlinien'
};

const counterpartPath = pathMap[currentPath] || (isEn ? '/kontakt' : '/en/contact');
const langDeHref = isEn ? counterpartPath : currentPath;
const langEnHref = isEn ? currentPath : counterpartPath;

const siteYear = new Date().getFullYear();
const canonicalURL = new URL(Astro.url.pathname, 'https://www.bifode.org');
const orgJsonLd = JSON.stringify({
  '@context': 'https://schema.org',
  '@type': 'Organization',
  name: 'BiFoDe e.V.',
  alternateName: 'Bildungsforum für Demokratie und Vielfalt NRW e.V.',
  url: 'https://www.bifode.org/',
  logo: 'https://www.bifode.org/logos/logo-light.svg',
  email: 'info@bifode.org',
});
---

<!doctype html>
<html lang={isEn ? 'en' : 'de'}>
        <head>
                <meta charset="UTF-8" />
                <meta name="viewport" content="width=device-width" />
                <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
                <link rel="icon" href="/favicon.ico" />
                <link rel="apple-touch-icon" href="/favicon.svg" />
                <meta name="generator" content={Astro.generator} />
                <title>{titleSuffix ? `${title} | BiFoDe` : title}</title>
                <meta name="description" content={description} />
                <link rel="canonical" href={canonicalURL} />
                <script type="application/ld+json" set:html={orgJsonLd} />

                <!-- Critical CSS - Inline for faster initial render (above-the-fold) -->
                <style>
                        *, *::before, *::after { box-sizing: border-box; }
                        html, body { margin: 0; padding: 0; height: 100%; font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif; background-color: #f9fafb; color: #111827; scroll-behavior: smooth; }
                        .app-container { display: flex; flex-direction: column; min-height: 100vh; }
                        .container { max-width: 1200px; margin: 0 auto; padding: 0 1rem; }
                        .navbar { background: white; border-bottom: 1px solid #e5e7eb; position: sticky; top: 0; z-index: 50; }
                        .nav-wrapper { display: flex; justify-content: space-between; align-items: center; padding: 1rem 16px; }
                        .logo { display: flex; align-items: center; gap: 0.5rem; text-decoration: none; color: #111827; font-weight: 700; font-size: 1.5rem; }
                        .logo-img { height: 36px; width: auto; }
                        .logo-light { display: block; }
                        .logo-dark { display: none; }
                        .nav-links { display: flex; gap: 1.15rem; }
                        .nav-links a { color: #6b7280; text-decoration: none; font-weight: 500; }
                        .burger-menu { display: none; flex-direction: column; background: none; border: none; cursor: pointer; padding: 0.5rem; gap: 0.4rem; }
                        .burger-line { width: 24px; height: 3px; background-color: #111827; border-radius: 2px; transition: all 0.3s ease; }
                        .main-content { flex: 1; padding: 2rem 0; }
                        @media (max-width: 768px) {
                                .burger-menu { display: flex; }
                                .nav-links { position: absolute; top: 100%; left: 0; right: 0; background: white; flex-direction: column; padding: 1rem; gap: 0; border-bottom: 1px solid #e5e7eb; max-height: 0; overflow: hidden; transition: max-height 0.3s ease; }
                                .navbar.is-open .nav-links { max-height: calc(100vh - 72px); overflow-y: auto; }
                                .nav-links a { padding: 0.75rem 1rem; border-bottom: 1px solid #f3f4f6; }
                        }
                </style>
        </head>
        <body>
                <a href="#main-content" class="skip-link">{isEn ? 'Skip to main content' : 'Zum Inhalt springen'}</a>
                <div class="app-container">
                        <nav class="navbar" id="navbar">
                                <div class="container nav-wrapper">
                                        <a href={isEn ? '/en' : '/'} class="logo">
                                                <img src="/logos/logo-light.svg" alt="BiFoDe Logo" class="logo-img logo-light" width="1896" height="484" />
                                        </a>
                                        <div class="nav-links" id="navLinks">
                                                {navStructure.map((entry) =>
                                                  entry.kind === 'link' ? (
                                                    <a
                                                      href={entry.href}
                                                      class:list={['nav-top-link', entry.external && 'nav-top-link--external']}
                                                      data-nav-link
                                                      {...(entry.external
                                                        ? {
                                                            target: '_blank',
                                                            rel: 'noopener noreferrer',
                                                            'aria-label': `${entry.label} (Facebook)`
                                                          }
                                                        : {})}
                                                    >
                                                      {entry.label}
                                                    </a>
                                                  ) : (
                                                    <details class="nav-drop">
                                                      <summary class="nav-drop-summary">
                                                        <span>{entry.label}</span>
                                                        <span class="nav-drop-chevron" aria-hidden="true" />
                                                      </summary>
                                                      <div class="nav-drop-panel">
                                                        {entry.items.map((item) => (
                                                          <a
                                                            href={item.href}
                                                            data-nav-link
                                                            {...(item.external
                                                              ? { target: '_blank', rel: 'noopener noreferrer' }
                                                              : {})}
                                                          >
                                                            {item.label}
                                                          </a>
                                                        ))}
                                                      </div>
                                                    </details>
                                                  )
                                                )}
                                                <div class="lang-switch" id="langSwitch">
                                                        <a href={langDeHref} id="langDe" class="lang-link">DE</a>
                                                        <span class="lang-divider">|</span>
                                                        <a href={langEnHref} id="langEn" class="lang-link">EN</a>
                                                </div>
                                        </div>
                                        <div class="nav-controls">
                                                <div class="lang-switch-mobile">
                                                        <a href={langDeHref} class="lang-link" aria-current={isEn ? undefined : 'page'}>DE</a>
                                                        <span class="lang-divider">|</span>
                                                        <a href={langEnHref} class="lang-link" aria-current={isEn ? 'page' : undefined}>EN</a>
                                                </div>
                                                <button class="burger-menu" id="burgerMenu" aria-label="Navigation menu" aria-expanded="false">
                                                        <span class="burger-line"></span>
                                                        <span class="burger-line"></span>
                                                        <span class="burger-line"></span>
                                                </button>
                                        </div>
                                </div>
                        </nav>

                        <main id="main-content" class:list={['main-content', mainClass]} tabindex="-1">
                                <Breadcrumbs items={breadcrumbItems} lang={locale} />
                                <slot />
                        </main>

                        <footer class="footer">
                                <div class="container footer-shell">
                                        <div class="footer-brand">
                                                <a href={isEn ? '/en' : '/'} class="footer-logo">
                                                        <img src="/logos/logo-dark.svg" alt="BiFoDe Logo" width="1866" height="666" loading="lazy" />
                                                </a>
                                                <p class="footer-brand-text">{footerBrandText}</p>
                                        </div>

                                        <div class="footer-columns">
                                                <div class="footer-col">
                                                        <h4>{isEn ? 'Organisation' : 'Organisation'}</h4>
                                                        <ul>
                                                                {footerOrganisation.map((item) => (
                                                                        <li>
                                                                                <a href={item.href}>{item.label}</a>
                                                                        </li>
                                                                ))}
                                                        </ul>
                                                </div>

                                                <div class="footer-col">
                                                        <h4>{isEn ? 'Programmes' : 'Angebote'}</h4>
                                                        <ul>
                                                                {footerOffers.map((item) => (
                                                                        <li>
                                                                                <a href={item.href}>{item.label}</a>
                                                                        </li>
                                                                ))}
                                                        </ul>
                                                </div>

                                                <div class="footer-col">
                                                        <h4>{isEn ? 'Updates' : 'Aktuell'}</h4>
                                                        <ul>
                                                                {footerCurrent.map((item) => (
                                                                        <li>
                                                                                <a
                                                                                        href={item.href}
                                                                                        {...(item.external
                                                                                                ? { target: '_blank', rel: 'noopener noreferrer' }
                                                                                                : {})}
                                                                                >
                                                                                        {item.label}{item.external ? ' ↗' : ''}
                                                                                </a>
                                                                        </li>
                                                                ))}
                                                        </ul>
                                                </div>

                                                <div class="footer-col footer-col-join">
                                                        <h4>{isEn ? 'Get involved' : 'Mitmachen'}</h4>
                                                        <ul class="footer-cta-list">
                                                                {footerJoin.map((item) => (
                                                                        <li>
                                                                                <a href={item.href} class:list={[item.cta && 'footer-cta']}>
                                                                                        {item.label}
                                                                                </a>
                                                                        </li>
                                                                ))}
                                                        </ul>
                                                </div>

                                                <div class="footer-col footer-col-contact">
                                                        <h4>{isEn ? 'Contact & legal' : 'Kontakt & Rechtliches'}</h4>
                                                        <ul class="footer-contact-stack">
                                                                <li>
                                                                        <a href="mailto:info@bifode.org">info@bifode.org</a>
                                                                </li>
                                                                <li>
                                                                        <a href={isEn ? '/en/contact' : '/kontakt'}>
                                                                                {isEn ? 'Contact page' : 'Kontaktseite'}
                                                                        </a>
                                                                </li>
                                                                <li class="footer-stack-gap" aria-hidden="true"></li>
                                                                {footerLegalLinks.map((item) => (
                                                                        <li>
                                                                                <a href={item.href}>{item.label}</a>
                                                                        </li>
                                                                ))}
                                                                <li>
                                                                        <button type="button" class="footer-cookie-button" id="footer-cookie-settings">
                                                                                {isEn ? 'Cookie Settings' : 'Cookie-Einstellungen'}
                                                                        </button>
                                                                </li>
                                                                <li class="footer-stack-gap" aria-hidden="true"></li>
                                                                <li>
                                                                        <a
                                                                                href={FB_PAGE_BIFODE}
                                                                                target="_blank"
                                                                                rel="noopener noreferrer"
                                                                                class="footer-social-link"
                                                                        >
                                                                                Facebook ↗
                                                                        </a>
                                                                </li>
                                                        </ul>
                                                </div>
                                        </div>
                                </div>

                                <div class="container footer-legal-note">
                                        <p>{footerDisclaimer}</p>
                                </div>

                                <div class="footer-bottom">
                                        <div class="container">
                                                <p>&copy; {siteYear} BiFoDe e.V. {isEn ? 'All rights reserved.' : 'Alle Rechte vorbehalten.'}</p>
                                        </div>
                                </div>
                        </footer>
                </div>

                <!-- Cookie Banner -->
                <CookieBanner lang={locale} />

                <FloatingCta lang={isEn ? 'en' : 'de'} />

                <script>
                        document.addEventListener('DOMContentLoaded', () => {
                                const burgerMenu = document.getElementById('burgerMenu');
                                const navbar = document.getElementById('navbar');
                                const navLinks = document.getElementById('navLinks');
                                if (!burgerMenu || !navbar || !navLinks) return;

                                const navItems = navLinks.querySelectorAll('[data-nav-link]');
                                const currentPath = window.location.pathname.replace(/\/$/, '') || '/';
                                const langDe = document.getElementById('langDe');
                                const langEn = document.getElementById('langEn');

                                navItems.forEach(item => {
                                        const linkPath = (item.getAttribute('href') || '').replace(/\/$/, '') || '/';
                                        if (linkPath === currentPath) {
                                                item.setAttribute('aria-current', 'page');
                                        }
                                });

                                navLinks.querySelectorAll('details.nav-drop').forEach(details => {
                                        const sub = details.querySelectorAll('.nav-drop-panel a[data-nav-link]');
                                        let match = false;
                                        sub.forEach(a => {
                                                const p = (a.getAttribute('href') || '').replace(/\/$/, '') || '/';
                                                if (p === currentPath) match = true;
                                        });
                                        if (match) details.classList.add('nav-drop--current');
                                });

                                if (langDe && langEn) {
                                        if (currentPath.startsWith('/en')) {
                                                langEn.setAttribute('aria-current', 'page');
                                        } else {
                                                langDe.setAttribute('aria-current', 'page');
                                        }
                                }

                                // Toggle menu on burger click
                                burgerMenu.addEventListener('click', () => {
                                        navbar.classList.toggle('is-open');
                                        burgerMenu.classList.toggle('is-open');
                                        burgerMenu.setAttribute('aria-expanded', burgerMenu.classList.contains('is-open') ? 'true' : 'false');
                                });

                                // Close menu when a link is clicked; collapse dropdowns
                                const closeMobileNav = () => {
                                        navLinks.querySelectorAll('details.nav-drop').forEach(d => d.removeAttribute('open'));
                                        navbar.classList.remove('is-open');
                                        burgerMenu.classList.remove('is-open');
                                        burgerMenu.setAttribute('aria-expanded', 'false');
                                };

                                navItems.forEach(item => {
                                        item.addEventListener('click', closeMobileNav);
                                });

				const closeDropdowns = () => {
					navLinks.querySelectorAll('details.nav-drop[open]').forEach(d => d.removeAttribute('open'));
				};

				// Close menu/dropdowns when clicking outside
				document.addEventListener('click', (event) => {
					const target = event.target;
					if (!(target instanceof Node)) return;
					if (!navbar.contains(target)) {
						closeDropdowns();
						if (navbar.classList.contains('is-open')) {
							navbar.classList.remove('is-open');
							burgerMenu.classList.remove('is-open');
							burgerMenu.setAttribute('aria-expanded', 'false');
						}
					}
				});

				// Only one desktop dropdown open at a time
				navLinks.querySelectorAll('details.nav-drop').forEach(details => {
					details.addEventListener('toggle', () => {
						if (!details.open) return;
						navLinks.querySelectorAll('details.nav-drop[open]').forEach(other => {
							if (other !== details) other.removeAttribute('open');
						});
					});
				});

				// Escape closes dropdowns and mobile menu
				document.addEventListener('keydown', (event) => {
					if (event.key !== 'Escape') return;
					closeDropdowns();
					if (navbar.classList.contains('is-open')) {
						navbar.classList.remove('is-open');
						burgerMenu.classList.remove('is-open');
						burgerMenu.setAttribute('aria-expanded', 'false');
						burgerMenu.focus();
					}
				});

                                const footerCookieSettings = document.getElementById('footer-cookie-settings');
                                footerCookieSettings?.addEventListener('click', () => {
                                        window.dispatchEvent(new CustomEvent('open-cookie-settings'));
                                });
                        });
                </script>
        </body>
</html>

<style is:global>
        *, *::before, *::after {
                box-sizing: border-box;
        }

	:root {
		--bg: #f4f7fb;
		--surface: #ffffff;
		--surface-soft: #f8fbff;
		--text: #0f172a;
		--muted: #4b5563;
		--line: #dbe5f1;

		/* Brand: navy = primary, royal blue = interactive accent */
		--primary: #163b63;
		--primary-strong: #0f2e50;
		--accent: #1d4ed8;
		--accent-strong: #1e40af;
		--accent-tint: rgba(29, 78, 216, 0.12);

		/* Filled CTA */
		--cta-grad: linear-gradient(135deg, #1d4ed8 0%, #163b63 100%);
		--cta-grad-hover: linear-gradient(135deg, #1e40af 0%, #0f2e50 100%);
		--cta-shadow: 0 10px 24px rgba(29, 78, 216, 0.3);
		--cta-shadow-hover: 0 14px 30px rgba(29, 78, 216, 0.34);

		--shadow: 0 14px 34px rgba(16, 34, 62, 0.08);
		--shadow-card-hover: 0 18px 45px rgba(15, 42, 69, 0.13);

		--radius-sm: 10px;
		--radius-md: 14px;
		--radius-lg: 18px;
		--radius: 18px;
	}

        html, body {
                margin: 0;
                padding: 0;
                height: 100%;
                font-family: 'Inter', sans-serif;
                background-color: var(--bg);
                color: var(--text);
                scroll-behavior: smooth;
        }

        .skip-link {
                position: absolute;
                left: 0;
                top: 0;
                z-index: 200;
                padding: 0.65rem 1.1rem;
                background: var(--primary);
                color: #fff !important;
                font-weight: 600;
                font-size: 0.9rem;
                text-decoration: none;
                border-radius: 0 0 10px 0;
                transform: translateY(-120%);
                transition: transform 0.2s ease;
        }

        .skip-link:focus {
                outline: none;
                transform: translateY(0);
        }

        .skip-link:focus-visible {
                outline: 2px solid #fff;
                outline-offset: 2px;
        }

        .app-container {
                display: flex;
                flex-direction: column;
                min-height: 100vh;
        }

        .container {
                max-width: 1140px;
                margin: 0 auto;
                padding: 0 1.25rem;
        }

        .navbar {
                background: rgba(255, 255, 255, 0.92);
                backdrop-filter: blur(8px);
                border-bottom: 1px solid var(--line);
                position: sticky;
                top: 0;
                z-index: 50;
        }

        .nav-wrapper {
                display: flex;
                align-items: center;
                gap: 0.75rem 1rem;
                padding: 1rem 0;
        }

        .logo {
                display: flex;
                align-items: center;
                gap: 0.5rem;
                text-decoration: none;
                color: var(--text);
                font-weight: 700;
                font-size: 1.5rem;
        }

        .logo:hover .logo-img {
                transform: scale(1.03);
                transition: transform 0.2s ease;
        }

        .logo-img {
                height: 36px;
                width: auto;
        }

        .logo-light {
                display: block;
        }

        .logo-dark {
                display: none;
        }

        .dark .logo-light {
                display: none;
        }

        .dark .logo-dark {
                display: block;
        }

        .nav-links {
                display: flex;
                align-items: center;
                flex-wrap: wrap;
                justify-content: flex-end;
                gap: 0.3rem 0.9rem;
                flex: 1;
                min-width: 0;
        }

        @media (min-width: 1025px) and (max-width: 1200px) {
                .nav-links {
                        gap: 0.25rem 0.65rem;
                }

                .nav-links .nav-top-link,
                .nav-drop-summary {
                        font-size: 0.9rem;
                }
        }

        .nav-links .nav-top-link,
        .nav-links .nav-drop-panel a {
                color: var(--muted);
                text-decoration: none;
                font-weight: 500;
                transition: color 0.2s, border-color 0.2s, background-color 0.2s;
                padding-bottom: 0.3rem;
                border-bottom: 2px solid transparent;
                white-space: nowrap;
        }

        .nav-links .nav-top-link:hover,
        .nav-links .nav-drop-panel a:hover {
                color: var(--primary);
        }

        .nav-links .nav-top-link[aria-current='page'],
        .nav-links .nav-drop-panel a[aria-current='page'] {
                color: var(--primary);
                border-bottom-color: var(--primary);
        }

        .nav-top-link--external::after {
                content: '↗';
                font-size: 0.72em;
                margin-left: 0.15em;
                opacity: 0.75;
        }

        .nav-drop {
                position: relative;
        }

        .nav-drop-summary {
                list-style: none;
                cursor: pointer;
                display: inline-flex;
                align-items: center;
                gap: 0.35rem;
                color: var(--muted);
                font-weight: 500;
                font-size: inherit;
                font-family: inherit;
                padding: 0 0 0.3rem;
                margin: 0;
                border: 0;
                border-bottom: 2px solid transparent;
                background: transparent;
                user-select: none;
                white-space: nowrap;
                transition: color 0.2s, border-color 0.2s;
        }

        .nav-drop-summary::-webkit-details-marker {
                display: none;
        }

        .nav-drop-summary::marker {
                content: '';
        }

        .nav-drop-summary:hover,
        .nav-drop-summary:focus-visible {
                color: var(--primary);
                outline: none;
        }

        .nav-drop[open] > .nav-drop-summary,
        .nav-drop.nav-drop--current > .nav-drop-summary {
                color: var(--primary);
                border-bottom-color: var(--primary);
        }

        .nav-drop-chevron {
                display: inline-block;
                width: 0;
                height: 0;
                border-left: 4px solid transparent;
                border-right: 4px solid transparent;
                border-top: 5px solid currentColor;
                margin-top: 2px;
                transition: transform 0.2s ease;
        }

        .nav-drop[open] .nav-drop-chevron {
                transform: rotate(180deg);
        }

        .nav-drop-panel {
                position: absolute;
                top: calc(100% + 0.35rem);
                left: 0;
                min-width: 12.5rem;
                padding: 0.4rem 0;
                margin: 0;
                background: var(--surface);
                border: 1px solid var(--line);
                border-radius: 14px;
                box-shadow: var(--shadow);
                z-index: 80;
                display: flex;
                flex-direction: column;
                gap: 0.1rem;
        }

        .nav-drop-panel a {
                display: block;
                padding: 0.5rem 1rem;
                margin: 0 0.35rem;
                border-radius: 8px;
                border-bottom: none;
        }

        .nav-drop-panel a:hover {
                background: var(--surface-soft);
        }

        .nav-drop-summary:focus-visible,
        .nav-top-link:focus-visible,
        .nav-drop-panel a:focus-visible,
        .lang-link:focus-visible,
        .burger-menu:focus-visible {
                outline: 2px solid var(--primary);
                outline-offset: 2px;
        }

        .lang-switch {
                display: inline-flex;
                align-items: center;
                gap: 0.4rem;
                margin-left: 0.5rem;
                padding-left: 0.25rem;
                border-left: 1px solid var(--line);
        }

        .lang-link {
                color: var(--muted);
                text-decoration: none;
                font-weight: 700;
                font-size: 0.85rem;
                letter-spacing: 0.04em;
        }

        .lang-link[aria-current='page'] {
                color: var(--primary);
        }

        .lang-divider {
                color: #94a3b8;
                font-size: 0.8rem;
        }

        .nav-controls {
                display: inline-flex;
                align-items: center;
                gap: 0.9rem;
        }

        .lang-switch-mobile {
                display: none;
                align-items: center;
                gap: 0.35rem;
        }

        /* Burger Menu Button */
        .burger-menu {
                display: none;
                flex-direction: column;
                background: none;
                border: none;
                cursor: pointer;
                padding: 0.5rem;
                gap: 0.4rem;
        }

        .burger-line {
                width: 24px;
                height: 3px;
                background-color: var(--text);
                border-radius: 2px;
                transition: all 0.3s ease;
        }

        .burger-menu.is-open .burger-line:nth-child(1) {
                transform: rotate(45deg) translate(8px, 8px);
        }

        .burger-menu.is-open .burger-line:nth-child(2) {
                opacity: 0;
        }

        .burger-menu.is-open .burger-line:nth-child(3) {
                transform: rotate(-45deg) translate(7px, -7px);
        }

        .main-content {
                flex: 1;
                padding: 2.5rem 0 3.5rem;
        }

        .footer {
                background: linear-gradient(180deg, #122a45 0%, #0f2439 100%);
                color: #b8c5d6;
                padding: 3.25rem 0 1.75rem;
        }

        .footer-shell {
                display: grid;
                grid-template-columns: minmax(200px, 260px) minmax(0, 1fr);
                gap: 2.5rem 3rem;
                align-items: start;
        }

        .footer-columns {
                display: grid;
                grid-template-columns: repeat(5, minmax(0, 1fr));
                gap: 1.75rem 2rem;
        }

        .footer-brand-text {
                margin: 0;
                font-size: 0.9rem;
                line-height: 1.65;
                color: #94a3b8;
                max-width: 28rem;
        }

        .footer-legal-note {
                margin-top: 2rem;
                padding-top: 1.25rem;
                border-top: 1px solid rgba(255, 255, 255, 0.08);
        }

        .footer-legal-note p {
                margin: 0;
                font-size: 0.78rem;
                line-height: 1.55;
                color: #64748b;
                max-width: 72rem;
        }

        .footer-logo {
                display: inline-block;
                margin-bottom: 1.15rem;
        }

        .footer-logo img {
                height: 36px;
                width: auto;
                opacity: 0.92;
        }

        .footer h4 {
                color: #f1f5f9;
                font-size: 0.82rem;
                font-weight: 600;
                letter-spacing: 0.02em;
                margin: 0 0 0.85rem;
        }

        .footer ul {
                list-style: none;
                padding: 0;
                margin: 0;
        }

        .footer-col li {
                margin-bottom: 0.55rem;
        }

        .footer-col li:last-child {
                margin-bottom: 0;
        }

        .footer a {
                color: #94a3b8;
                text-decoration: none;
                font-size: 0.9rem;
                line-height: 1.45;
                transition: color 0.2s ease;
        }

        .footer-cookie-button {
                background: transparent;
                border: none;
                padding: 0;
                color: #94a3b8;
                font: inherit;
                font-size: 0.9rem;
                cursor: pointer;
                text-align: left;
        }

        .footer a:hover,
        .footer-cookie-button:hover {
                color: #ffffff;
        }

        .footer-cta-list {
                display: flex;
                flex-direction: column;
                gap: 0.65rem;
        }

        .footer-cta-list li {
                margin-bottom: 0;
        }

        .footer-cta {
                display: inline-flex;
                align-items: center;
                padding: 0.42rem 0.7rem;
                border: 1px solid rgba(255, 255, 255, 0.28);
                border-radius: 6px;
                color: #e2e8f0 !important;
                font-weight: 600;
                font-size: 0.84rem !important;
                transition: background 0.2s ease, border-color 0.2s ease, color 0.2s ease;
        }

        .footer-cta:hover {
                background: rgba(255, 255, 255, 0.07);
                border-color: rgba(255, 255, 255, 0.45);
                color: #ffffff !important;
        }

        .footer-contact-stack .footer-stack-gap {
                height: 0;
                margin: 0.35rem 0 0.5rem;
                padding: 0;
                border-top: 1px solid rgba(255, 255, 255, 0.08);
                pointer-events: none;
        }

        .footer-social-link {
                font-size: 0.86rem !important;
                color: #64748b !important;
        }

        .footer-social-link:hover {
                color: #ffffff !important;
        }

        .footer-bottom {
                margin-top: 1.5rem;
                padding-top: 1rem;
                border-top: 1px solid rgba(255, 255, 255, 0.08);
                text-align: center;
                font-size: 0.82rem;
        }

        .footer-bottom p {
                color: #64748b;
                margin: 0;
        }

	@media (max-width: 1024px) {
		.nav-wrapper {
			flex-direction: row;
			justify-content: space-between;
			align-items: center;
			padding: 1rem 0;
		}

		.burger-menu {
			display: flex;
		}

                .nav-links {
                        position: absolute;
                        top: 100%;
                        left: 0;
                        right: 0;
                        background: white;
                        flex-direction: column;
                        padding: 1rem;
                        gap: 0;
                        border-bottom: 1px solid #e5e7eb;
                        max-height: 0;
                        overflow: hidden;
                        transition: max-height 0.3s ease;
                }

                .navbar.is-open .nav-links {
                        max-height: calc(100vh - 72px);
                        overflow-y: auto;
                }

                .nav-links .nav-top-link {
                        display: block;
                        width: 100%;
                        padding: 0.75rem 1rem;
                        border-bottom: 1px solid #f3f4f6;
                        border-radius: 0;
                        transition: background-color 0.2s ease, color 0.2s ease;
                }

                .nav-drop {
                        width: 100%;
                        border-bottom: 1px solid #f3f4f6;
                }

                .nav-drop-summary {
                        width: 100%;
                        padding: 0.75rem 1rem;
                        justify-content: space-between;
                        border-bottom: none;
                }

                .nav-drop-panel {
                        position: static;
                        min-width: 0;
                        width: 100%;
                        padding: 0 0 0.35rem;
                        margin: 0;
                        border: 0;
                        border-radius: 0;
                        box-shadow: none;
                        background: transparent;
                }

                .nav-drop-panel a {
                        padding: 0.55rem 1rem 0.55rem 1.75rem;
                        margin: 0;
                        border-radius: 0;
                        border-bottom: 1px solid #f9fafb;
                }

                .nav-drop-panel a:last-child {
                        border-bottom: none;
                }

                .lang-switch {
                        display: none;
                }

                .lang-switch-mobile {
                        display: inline-flex;
                }

                .nav-links {
                        flex: none;
                        justify-content: flex-start;
                }

                .nav-links .nav-top-link:hover,
                .nav-drop-panel a:hover {
                        background-color: #f9fafb;
                }

                .navbar {
                        position: relative;
                }

                .footer-shell {
                        grid-template-columns: 1fr;
                        gap: 2rem;
                }

                .footer-columns {
                        grid-template-columns: repeat(3, minmax(0, 1fr));
                        gap: 1.5rem 1.75rem;
                }

                .footer-col-contact {
                        grid-column: span 3;
                }
	}

        @media (max-width: 768px) {
                .footer-columns {
                        grid-template-columns: repeat(2, minmax(0, 1fr));
                        gap: 1.35rem 1.25rem;
                }

                .footer-col-contact {
                        grid-column: 1 / -1;
                }
        }

        @media (max-width: 640px) {
                .logo img.logo-main {
                        display: none;
                }

                .logo img.logo-icon {
                        display: block;
                }
        }

        @media (max-width: 600px) {
                .footer {
                        padding: 2.5rem 0 1.5rem;
                }

                .footer-columns {
                        grid-template-columns: 1fr;
                        gap: 1.5rem;
                }

                .footer-col-contact {
                        grid-column: auto;
                }

                .card h3,
                .feature-card h3 {
                        font-size: 1.1rem;
                }
        }

        /* Global Card Styles */
        .card-grid {
                display: grid;
                grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
                gap: 1.5rem;
        }

        .card,
        .feature-card {
                background: var(--surface);
                border: 1px solid var(--line);
                border-radius: var(--radius);
                padding: 1.75rem;
                box-shadow: var(--shadow);
                transition: transform 0.2s ease, box-shadow 0.2s ease;
        }

	.card:hover,
	.feature-card:hover {
		transform: translateY(-4px);
		box-shadow: var(--shadow-card-hover);
	}

        .card-icon {
                width: 56px;
                height: 56px;
                border-radius: 16px;
                background: linear-gradient(135deg, #e8f1ff, #f1f6ff);
                display: flex;
                align-items: center;
                justify-content: center;
                color: var(--primary);
                margin-bottom: 1rem;
                border: 1px solid #d8e5f8;
        }

        .card-icon svg {
                width: 24px;
                height: 24px;
                stroke: currentColor;
                fill: none;
                stroke-width: 1.8;
                stroke-linecap: round;
                stroke-linejoin: round;
        }

        .card h3,
        .feature-card h3 {
                margin: 0 0 0.75rem;
                font-size: 1.25rem;
                color: var(--text);
                line-height: 1.3;
                overflow-wrap: anywhere;
                hyphens: auto;
        }

        .card p,
        .feature-card p {
                margin: 0;
                color: var(--muted);
                line-height: 1.65;
        }

        .card-link {
                display: inline-flex;
                margin-top: 1.25rem;
                color: #163b63;
                font-weight: 700;
                text-decoration: none;
        }

        .card-link:hover {
                text-decoration: underline;
        }

	.btn-outline {
		display: inline-flex;
		align-items: center;
		margin-top: 1.25rem;
		padding: 0.6rem 1.2rem;
		border: 2px solid var(--primary);
		border-radius: var(--radius-sm);
		color: var(--primary);
		font-weight: 600;
		font-size: 0.95rem;
		text-decoration: none;
		background: transparent;
		transition: background 0.2s ease, color 0.2s ease, border-color 0.2s ease;
	}

	.btn-outline:hover {
		background: var(--primary);
		color: #ffffff;
		border-color: var(--primary);
	}

	@media (max-width: 768px) {
		html,
		body {
			background-color: var(--bg);
		}

		.main-content {
			padding: 1.5rem 0 2.25rem;
		}

		.card,
		.feature-card,
		.project-card,
		.contact-form-card,
		.contact-info,
		.steps .card {
			background: var(--surface);
			border: 1px solid var(--line);
			box-shadow: none;
			border-radius: var(--radius-md);
			padding: 1.1rem 1.15rem;
		}

		.card:hover,
		.feature-card:hover,
		.project-card:hover {
			transform: none;
			box-shadow: none;
		}

		.project-logic li {
			background: transparent;
			border: none;
			box-shadow: none;
			border-radius: 0;
			padding: 0;
		}

		.card-grid,
		.grid {
			gap: 0.85rem;
		}

		.card-icon {
			width: 40px;
			height: 40px;
			border-radius: 12px;
			margin-bottom: 0.65rem;
		}

		.card-icon svg {
			width: 20px;
			height: 20px;
		}
	}

        @media (max-width: 600px) {
                .card h3,
                .feature-card h3 {
                        font-size: 1.1rem;
                }
        }
</style>
