---
import { ULPAN_LANDING_PATH, jotformAnmeldungUrl } from '../lib/communityLinks';
const url = jotformAnmeldungUrl();
return Astro.redirect(url ? `${ULPAN_LANDING_PATH}#anmeldung` : ULPAN_LANDING_PATH, 302);
---
